# 看板读取的上游 mik HTTP 服务。默认就是本机 3211，一般不用改。
MIK_SERVER_URL=http://127.0.0.1:3211

# 只有 `mik serve --token <secret>` 启动时才需要填；留空表示不带 Authorization。
# 该值只在服务端使用，永远不会下发到浏览器。
# MIK_SERVER_TOKEN=

# seed 脚本用的数据库与应用标识（必须与 mik serve 一致，否则看板看不到数据）。
# MIK_DB=~/.model-infra-kit/usage.db
# MIK_APP_ID=default
