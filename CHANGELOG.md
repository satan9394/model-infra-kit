# CHANGELOG — model-infra-kit

> 本文件把 **42 个 Release / 41 个 npm 版本**（`v0.1.0` / `0.1.2` → `0.3.1`）压成可读的一条线。
> **权威细节**仍在 `docs/product-evolution.md`（每片留痕）与 GitHub Releases（逐版说明）；
> 本文件只回答「哪一版是什么、我该装哪个、为什么会有这么多版本」。

## 这条版本线怎么读（**先读这一段**）

**把 `0.1.x` – `0.3.1` 整体当成一段 `pre-1.0` 开发序列来读，而不是 42 次发布。** 这一段
（2026-09-09 → 09-12，四天）只有一个目的：把一个想法推成可用的东西。它没有对外承诺；
按 semver 的读法，**`0.x` 里的一切都可以在任何时刻变，事实也是如此**。

| 计数 | 实际值 | 说明 |
|---|---|---|
| GitHub Releases | **42** | `v0.1.0` … `v0.3.1` |
| npm 版本 | **41** | `0.1.2` … `0.3.1`（`v0.1.0`/`v0.1.1` 只发了 Release，未上 npm） |
| 有意义的产品状态 | **5 段** | 见下表：能装能跑 → 加固双语 → 承诺对齐实际 → 成本确定性可对账 → 弃用点估计 |
| 平均每个"段" | ≈ 8 个版本 | 这就是那个比值不好看的地方；**成因是流程，不是需求** |

**稳定线从这里开始 = `1.0.0`（已发布）**：它是**第一份对外承诺**——自此
破坏性变更走**主版本**，新增走次版本，修复走 patch；`pre-1.0` 这一段（`0.1.x` – `0.3.1`）只作历史保留，
**不再解释、不再维护**，npm 上已统一标注为 pre-1.0 开发序列。历史版本的正确读法就是「开发序列的第 N 次快照」。

**关于"为什么不合并"**：npm 与 GitHub **都没有 merge 已发布版本的操作**。能做的只有三件事——
**解释它**（本文件）、**标注它**（`npm deprecate`，可撤销、不删数据）、**删掉它**（`npm unpublish`
/ 删 Release，不可逆且会打断 pin 了旧版的用户）。本仓库选的是**解释 + 在稳定线上收口**；
**不删历史**，因为每个版本都对应一次真实修复，其 Release 说明就是那次修复的审计痕迹。

## 先说三件不体面但必须写清楚的事

1. **41 个 npm 版本全部诞生在 3 天内**：`0.1.2` 2026-09-10 10:42 → `0.3.1` 2026-09-12 20:32。
   其中 **9/11 一天 15 个**、**9/12 一天 20 个**；`0.1.3` 与 `0.1.4` 相隔 **23 秒**（同一次
   symlink 修复被拆成两版）；`v0.2.4` 这个版本号在 git 历史里**出现两次**。
2. **驱动它的不是需求，是流程**：当时的收尾规则是「**每完成一个切片就升一个 patch 并发布**」，
   于是版本号被当作记账单位，而不是「对外承诺的变化」。
3. **`0.3.1` 的发布物与 `0.3.0` 代码完全相同**——`packages/mik` 下只差 `package.json` 里那一行
   版本号（`git diff --stat 285e23a^ 285e23a -- packages/mik` 只有 1 文件 1 行）。它是一次
   「验证片也照规矩发版」的产物。

**已修正**：`AGENTS.md` 的发布闸门已收窄为「**只有当已发布包（`packages/mik`）的行为或公共接口
发生变化时才升版发布**；验证片 / 文档片 / 看板片只提交、不发版」。

## 装哪个

```bash
npm i model-infra-kit          # → 0.3.1（等价于 0.3.0 的代码）
```

`latest` = `0.3.1`。若你要固定版本，`0.3.0` 与 `0.3.1` **代码相同**，任选其一；`0.2.30` 及更早
缺 `0.3.0` 引入的「成本点估计弃用」这一层语义。

## 里程碑（41 版 → 5 段）

| 段 | 版本 | 天数 | 这一段真正交付了什么 |
|---|---|---|---|
| **A. 能装、能跑** | `0.1.2` – `0.1.9`（8 版） | 9/10–9/11 | 公开仓库安装、跨平台 netstat/CI、独立 REPL（双语斜杠命令）、首跑向导、`serve` 默认拒绝写 + SSRF 防护 + appId 伪造修复、CLI 调度解环 |
| **B. 加固与双语** | `0.2.0` – `0.2.10`（11 版） | 9/11 | 看板子进程治理（树杀、幂等）、脱敏加固（任意 auth 方案、不误杀散文）、SQLite 损坏自愈（隔离不删除）、成本对账文档 + 软预算告警、i18n 分文件 + OS locale、协议单一真源表 + 全仓环守卫、CLI 入门面本地化 |
| **C. 承诺对齐实际** | `0.2.11` – `0.2.23`（13 版） | 9/12 上午 | 首次调用路径可达（协议包提示、token 语义、编号引导）、子命令 `--help` 本地化、看板诚实性、供应商回传成本、未定价覆盖、EPIPE 不再崩、每件事只说一次、发布 README 链接可用、调用标签与按特性成本、库来源提示、第三道门禁诊断对齐 |
| **D. 成本确定性 + 可对账** | `0.2.24` – `0.2.30`（7 版） | 9/12 下午 | 未定价不再闭合成精确（「至少」下限 + 覆盖提示）、缺失值不再印成 `0 ms`、`init` 不再推荐装包后必失败的命令、`--cache-dir` 真正落盘、导出逐行可对账且防注入、终端可把一行导出对回一条 log、微美元 6 位、三处静默行为收敛 |
| **E. 弃用点估计** | `0.3.0` – `0.3.1`（2 版） | 9/12 晚 | `costUsd` 标记弃用并写清迁移（字段仍填充，不破坏读取方）；CLI 与看板的金额改由区间两端驱动。**`0.3.1` = `0.3.0` 代码**（见上） |
| **F. 收口（稳定线）** | `1.0.0`（1 版） | 09-13 | 把上面 5 段收成**一条对外承诺**：包内 README 写明稳定性（破坏性走主版本）、npm 上把 `0.1.x`–`0.3.1` 标注为 pre-1.0、GitHub Releases 归一化为一条 |

## 逐版一览

### A · `0.1.x`（8 版）
| 版本 | 时间 | 一句话 |
|---|---|---|
| `0.1.2` | 9/10 10:42 | 文档去个人化、可公开发布 |
| `0.1.3` / `0.1.4` | 9/10 11:25:42 / **11:26:05** | symlink bin 修复（**同一修复的两版，相隔 23 秒**） |
| `0.1.5` | 9/10 16:43 | 跨平台 netstat + 可移植 e2e python（CI 三 OS 绿） |
| `0.1.6` | 9/11 04:40 | `mik` REPL（双语斜杠命令、`/lang`、自由文本）+ 首跑向导 |
| `0.1.7` | 9/11 10:41 | G01 `serve` 默认拒绝写 + SSRF 守卫 + appId 伪造修复 |
| `0.1.8` | 9/11 11:11 | G02 REPL 提示键 / `/lang` 注入 / init 语言契约 |
| `0.1.9` | 9/11 14:17 | G03 抽出 `cli/dispatch.ts`，解掉 index↔repl 环 |

### B · `0.2.0` – `0.2.10`（11 版，均在 9/11）
| 版本 | 时间 | 一句话 |
|---|---|---|
| `0.2.0` | 14:36 | G04 看板子进程治理（树杀、幂等、退出兜底） |
| `0.2.1` | 15:14 | G05 脱敏加固 + 契约真值表 + 电池版本自检 |
| `0.2.2` | 15:28 | G06 SQLite 损坏自愈（隔离、永不删除）+ 句柄泄漏修复 |
| `0.2.3` | 15:56 | G07 成本对账文档 + 软预算告警（记量路径零 SQL） |
| `0.2.4` | 16:21 / 16:21 | G08 i18n 分语言文件 + OS locale 检测（**同号两笔提交**） |
| `0.2.5` | 16:57 | G09 首屏引导先于错误横幅 + 无 shell 启动看板 |
| `0.2.6` | 17:17 | G10 技术债轮（CLI i18n 覆盖、死导出裁定、电池重试） |
| `0.2.7` | 17:43 | G11 架构整理（协议单表、全仓环守卫、共享 CLI 前导） |
| `0.2.8` | 18:17 | G12 CLI 入门面本地化 + en 字节对等修复 |
| `0.2.9` | 18:58 | G13 `provider`/`usage` 输出本地化 + CJK 表格对齐 |
| `0.2.10` | 19:32 | G14 收口 CLI 层 i18n（`models`/`serve`/`pricing`/`dashboard`） |

### C · `0.2.11` – `0.2.23`（13 版，均在 9/12 上午）
| 版本 | 时间 | 一句话 |
|---|---|---|
| `0.2.11` | 03:08 | G15 首次调用路径可达（协议包提示、token 语义、编号引导） |
| `0.2.12` | 03:40 | G58 子命令 `--help` 选项说明本地化（285 键；19 个帮助面 en 字节一致） |
| `0.2.13` | 04:55 | G69 看板诚实性 + 接回 6 个死 i18n 键 |
| `0.2.14` | 05:40 | G73 供应商回传成本作为第六种价格来源 |
| `0.2.15` | 06:13 | G74 显示未定价覆盖，别让「成本看着正常」藏住盲区 |
| `0.2.16` | 07:58 | G76 读者提前关闭时 CLI 不再 EPIPE 崩溃 |
| `0.2.17` | 08:49 | G70 每件事只说一次；sqlite 警告不再嚷嚷 |
| `0.2.18` | 09:15 | G72 三件工具说真话 |
| `0.2.19` | 09:41 | G77 说明未定价数字看不全的场合 |
| `0.2.20` | 10:06 | G71 让发布包内 README 的链接真的可用 |
| `0.2.21` | 11:17 | G75 宿主可打标签，并按特性看成本 |
| `0.2.22` | 12:07 | G64 说明这些数字出自哪个库 |
| `0.2.23` | 13:36 | G72b 第三道门禁像另两道一样报出失败原因 |

### D · `0.2.24` – `0.2.30`（7 版，均在 9/12 下午）
| 版本 | 时间 | 一句话 |
|---|---|---|
| `0.2.24` | 14:41 | G78/G79/G80 未定价合计不再当作精确值 |
| `0.2.25` | 15:25 | G82 两处会被读错的渲染（换行伪造行、缺失值印成 `0 ms`） |
| `0.2.26` | 15:56 | G84 `init` 不再推荐装包后必失败的命令；`--cache-dir` 真正落盘 |
| `0.2.27` | 17:05 | G81 导出可对账；单元格不再能伪造一行 |
| `0.2.28` | 17:28 | G86 只在终端就能把一行导出对回一条 log |
| `0.2.29` | 17:54 | G85 显示导出本已能看见的微美元 |
| `0.2.30` | 18:49 | G88 三处静默行为收敛 |

### E · `0.3.x`（2 版，9/12 晚）
| 版本 | 时间 | 一句话 |
|---|---|---|
| `0.3.0` | 19:30 | G89 弃用成本**点估计**（`costUsd` 仍填充；CLI 与看板改由区间两端驱动） |
| `0.3.1` | 20:32 | G90 看板金额单元格的页面级断言 —— **无运行时变化；发布物与 `0.3.0` 代码相同** |

### F · `1.0.0`（**稳定线**，1 版）
| 版本 | 时间 | 一句话 |
|---|---|---|
| `1.0.0` | 09-13 | **把 pre-1.0 收口**：包内 README 写明稳定性承诺（破坏性走主版本）与 CHANGELOG 入口；npm 上把 `0.1.x`–`0.3.1` 统一标注为 pre-1.0 开发序列；GitHub Releases 归一化为**一条**（其说明即本文件的 5 段归纳）。自本版起按 semver 承诺。 |

## 未发布但在仓库里的

- **G91**（`trends` 金额单元格的页面级断言 + 6 位精度页面级取证）：已提交、CI 绿、**未升版**——
  正是新规则的第一个适用对象（它只动看板与 e2e）。
- **G92**（候选卡，未实现）：看板仍有 5 处成本格读被弃用的点估计，其中「最贵的一天」用点估计
  排序；根因在 `UsageTrendPoint`/`UsageBucket` 只有 `costUsd`，要动契约或产品语义。
