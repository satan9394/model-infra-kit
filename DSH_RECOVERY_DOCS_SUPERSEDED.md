# SUPERSEDED（已过期）：两份 DSH 恢复文档的归档说明

> **状态：SUPERSEDED（已过期）。** 本页是 `DSH_RECOVERY_CHECKPOINT.md` 与 `DSH_RECOVERY_REPORT.md` 的索引与墓碑页。
> 两份原件按「档案不许改写」的口径**原样入库、一字未改**，但其中的「现状 / 下一步」部分**不再代表当前事实，不得作为现状或行动依据引用**。
>
> 归档日期：2026-09-13。归档时基线：`main`，HEAD `e0e0705`（与 `origin/main` 同 SHA），已发布 `model-infra-kit@0.3.1`。

## 1. 两份文档各自的用途（一句话）

- **`DSH_RECOVERY_CHECKPOINT.md`**：某次自主长运行被紧急止损时的**现场快照**——记录当时的原始目标、已完成项、工作树状态与「恢复方式」。
- **`DSH_RECOVERY_REPORT.md`**：同一次救援的**结案报告**——汇总长运行成果、提交结果、风险与下一步；其 **§9/§10** 保存 G90（Dashboard 区间验证片）的一手证据，以及**自陈的唯一偏差**：「本轮没有独立 Evaluator（禁止子代理），所有结论为编排者自证」。

## 2. 为何已过期（分文档说明，勿一概而论）

### `DSH_RECOVERY_CHECKPOINT.md` —— 整体过期
它描述的现场状态（当时的 HEAD、在途改动、待裁决项）**已被后续提交消除或收编**：G90 已**实现 + 验收 PASS + 提交（`285e23a`）+ 文档收口（`e0e0705`）+ 发布（0.3.1）**。引用现状时**不要**读它。

### `DSH_RECOVERY_REPORT.md` —— **部分仍权威**
- **仍权威**：**§9/§10** 的 G90 一手证据与「无独立 Evaluator」的自陈——那是本轮补做独立核验的依据。
- **已过期**：§1/§2/§7 的「下一步/待办」——已被 `tasks/EVO-G90-dashboard-band-assertion.md`（追溯卡）与 `tasks/EVO-G91-trends-band-assertion.md`（下一轮候选）取代。

## 3. 该切片之后发生了什么（供交叉引用）

G90 收尾（`285e23a` + `e0e0705` + `0.3.1`）→ 本轮「确认与收口」Mission：① 冻结基线证据；② **实跑四类门禁**并落盘（`tsc --noEmit` ×2、mik 全量双 locale `694/39`、看板 `15/15`、e2e `12/12`，全部 exit 0）；③ 补上**缺失的 G90 追溯卡**（项目自己的「一卡一文件」纪律，`43d74e6` 教训）；④ 把「trends 单元格另一半盲区 + 金额 6 位精度页面级取证」**登记**为 `EVO-G91`（下一轮候选，本轮未实现）；⑤ 由**全新上下文独立核验** G90 的自证结论——补上它当初缺的那一环。

## 4. 引用与维护约定

- 引用**现状、风险或下一步**时：请用 `docs/product-evolution.md`（项目自述的唯一权威状态文件）、`AGENTS.md`、`tasks/EVO-G*.md`，**不要**引用这两份文档。
- 仅当需要**追溯那次长运行本身**（现场快照、G90 一手证据、自陈偏差）时才读原件。
- 两份原件**保持只读**：不改写、不追加、不移动。日后若结论更新，**新增文件**并在此页登记。
